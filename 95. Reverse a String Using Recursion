#include <stdio.h>

void reverse(char s[], int i, int j) {
    if(i >= j) return;
    char t = s[i];
    s[i] = s[j];
    s[j] = t;
    reverse(s, i+1, j-1);
}

int main() {
    char s[100];
    gets(s);

    reverse(s, 0, strlen(s)-1);

    printf("Reversed: %s", s);
    return 0;
}
