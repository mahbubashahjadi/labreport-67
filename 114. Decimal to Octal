
#include <stdio.h>

int main() {
    int n, octal = 0, base = 1;
    scanf("%d", &n);

    while(n > 0) {
        octal += (n % 8) * base;
        n /= 8;
        base *= 10;
    }
    printf("%d", octal);
    return 0;
}
