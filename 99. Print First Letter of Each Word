
#include <stdio.h>

int main() {
    char s[200];
    gets(s);

    if(s[0] != ' ') printf("%c ", s[0]);

    for(int i=1; s[i]; i++)
        if(s[i-1] == ' ' && s[i] != ' ')
            printf("%c ", s[i]);

    return 0;
}
