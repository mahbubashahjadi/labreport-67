
#include <stdio.h>

int main() {
    FILE *in = fopen("input.txt", "r");
    FILE *out = fopen("output.txt", "w");

    char c;
    while((c = fgetc(in)) != EOF)
        fputc(c, out);

    fclose(in);
    fclose(out);

    printf("Copied successfully.");
    return 0;
}
