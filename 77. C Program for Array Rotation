#include <stdio.h>

void leftRotateByOne(int a[], int n) {
    int first = a[0];
    for(int i=0;i<n-1;i++) a[i] = a[i+1];
    a[n-1] = first;
}

void leftRotate(int a[], int n, int d) {
    d = d % n;
    for(int i=0;i<d;i++) leftRotateByOne(a,n);
}

int main() {
    int n; scanf("%d",&n);
    int a[n]; for(int i=0;i<n;i++) scanf("%d",&a[i]);
    int d; scanf("%d",&d);
    leftRotate(a,n,d);
    for(int i=0;i<n;i++) printf("%d ", a[i]);
    printf("\n");
    return 0;
}
