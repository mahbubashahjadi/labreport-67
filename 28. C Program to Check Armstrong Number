

(153 → Armstrong)

#include <stdio.h>
#include <math.h>

int main() {
    int n, temp, sum = 0, digits = 0;
    scanf("%d", &n);

    temp = n;

    while(temp != 0) {
        digits++;
        temp /= 10;
    }
