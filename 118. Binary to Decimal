
#include <stdio.h>

int main() {
    long bin;
    scanf("%ld", &bin);

    int dec = 0, base = 1;

    while(bin > 0) {
        dec += (bin % 10) * base;
        base *= 2;
        bin /= 10;
    }

    printf("%d", dec);
    return 0;
}
