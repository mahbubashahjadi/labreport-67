#include <stdio.h>

int main() {
    int n; scanf("%d",&n);
    int a[n]; for(int i=0;i<n;i++) scanf("%d",&a[i]);
    int x; scanf("%d",&x);
    int idx = 0;
    for(int i=0;i<n;i++) if(a[i] != x) a[idx++] = a[i];
    for(int i=0;i<idx;i++) printf("%d ", a[i]);
    printf("\n");
    return 0;
}
