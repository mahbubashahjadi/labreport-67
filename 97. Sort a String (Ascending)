97. Sort a String (Ascending)
#include <stdio.h>
#include <string.h>

int main() {
    char s[100];
    gets(s);

    int n = strlen(s);

    for(int i=0;i<n;i++) {
        for(int j=i+1;j<n;j++) {
            if(s[j] < s[i]) {
                char t = s[j];
                s[j] = s[i];
                s[i] = t;
            }
        }
    }

    printf("Sorted: %s", s);
    return 0;
}
