#include <stdio.h>
#include <math.h>

int main() {
    int low, high, num, temp, digits, r, sum;

    scanf("%d %d", &low, &high);

    for(num = low; num <= high; num++) {
        temp = num;
        digits = 0;
        sum = 0;

        while(temp != 0) {
            digits++;
            temp /= 10;
        }

        temp = num;
        while(temp != 0) {
            r = temp % 10;
            sum += pow(r, digits);
            temp /= 10;
        }

        if(sum == num)
            printf("%d ", num);
    }

    return 0;
}
