
#include <stdio.h>

int main() {
    char a[100], b[100];
    gets(a);
    gets(b);

    int i=0;

    while(a[i] && b[i]) {
        if(a[i] != b[i]) {
            printf("Not Equal");
            return 0;
        }
        i++;
    }
