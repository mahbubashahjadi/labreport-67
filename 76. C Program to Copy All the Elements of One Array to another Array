#include <stdio.h>

int main() {
    int n; scanf("%d",&n);
    int src[n], dest[n];
    for(int i=0;i<n;i++) scanf("%d",&src[i]);
    for(int i=0;i<n;i++) dest[i] = src[i];
    for(int i=0;i<n;i++) printf("%d ", dest[i]);
    printf("\n");
    return 0;
}
