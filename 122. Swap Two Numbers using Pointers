
#include <stdio.h>

int main() {
    int a, b;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    int *p = &a, *q = &b;

    int temp = *p;
    *p = *q;
    *q = temp;

    printf("After swap: %d %d", a, b);
    return 0;
}
